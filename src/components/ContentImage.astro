---
interface Props {
  path: string;
  width?: number;
}

// biome-ignore lint/correctness/noUnusedVariables: used in template
const { path, width } = Astro.props as Props;
---

<img
  src={`${import.meta.env.BASE_URL}/content/images/${path}`}
  width={width}
/>
